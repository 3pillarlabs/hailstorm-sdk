# frozen_string_literal: true

require 'hailstorm/model'

# Model helpers are responsible for `how' the model executes low level functions. A model will instantiate
# the helper and call available methods. In a service based architecture, a helper would be injected into
# the service instance.
module Hailstorm::Model::Helper
end
